name: stream_mixin
description: State management using stream to avoid use of flutter's State/setState/StatefulWidget, boosting the performance and code separation.
version: 3.0.4
homepage: https://github.com/arnold-parge/stream_mixin

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

flutter: